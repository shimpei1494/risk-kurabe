# ハッカソン版の実装制約

- 開発期間: 2週間
- 開発者: 1人
- 開発支援: AIを活用
- 目的: ハッカソンで動作するMVPを完成・公開する
- 技術基盤: 既存のTanStack Start、React、TypeScript、MantineおよびVite+を維持する
- 配置方針: 可能な限りCloudflareを利用し、低コストに抑える
- データ方針: ハッカソン期間では固定データスナップショットを使用する

## Cloudflare環境

- ハッカソン用アカウント: `tokyo_odh_044`
- Account ID: `53af804e239e6294ad9a766add0c6e00`
- Wrangler認証: 開発者ごとのローカル認証プロファイルを使用し、認証情報をGitへ保存しない
- 誤操作防止: `wrangler.jsonc` の `account_id` でデプロイ先をハッカソン用アカウントに固定する
- Worker: `https://risk-kurabe.tokyo-odh-044.workers.dev`
- R2バケット: `risk-kurabe-data`
- R2公開データ: `https://pub-bc1c84661928416fbcde6535c9039c50.r2.dev/risk-data/v3/`
- Worker Secrets: `YAHOO_CLIENT_ID`、`CF_AIG_TOKEN`（値はCloudflare上だけに保存する）

2026-07-30時点で、ハッカソン用OAuthトークンには次の操作権限が付与されている。

| 分類 | 利用可能な機能・権限 |
| --- | --- |
| アカウント | Account Read、User Read、Zone Read |
| Workers | Workers Write、Scripts Write、Routes Write、Tail Read |
| ストレージ・データ | Workers KV Write、R2、D1 Write |
| 配信 | Pages Write、SSL Certificates Write |
| AI | Workers AI Write、AI Search Write/Run、Web Search Run、Agent Memory Write |
| 非同期処理 | Queues Write、Pipelines Write |
| 開発基盤 | Secrets Store Write、Artifacts Write、Flagship Write |
| コンピュート・接続 | Containers Write、Cloudchamber Write、Connectivity Admin |
| その他 | Email Routing Write、Email Sending Write、Browser Write、Challenge Widgets Write |

現在、洪水はハザードマップポータルサイトの公式統合タイルを実行時に参照し、R2の
`risk-data/v3`には東京都地域危険度のFlatGeobuf、PMTilesとメタデータだけを配置する。
HTTP Rangeの`206 Partial Content`、本番Origin向けCORS、Workerの`200`応答および
ブラウザの主要導線を確認してから公開する。

R2のPublic Development URLはレート制限のある開発用エンドポイントである。ハッカソン版では利用可能だが、長期運用へ移行する場合はR2カスタムドメインを検討する。

ここでいう「利用可能」は現在のOAuth認証で操作が許可されていることを表す。各機能のプラン、クォータ、ハッカソンの利用規約および管理者側の制限は別途適用される。実装では必要な機能だけを使用し、権限があることだけを理由に構成を拡大しない。

## 計画上の原則

- 公開データに基づく地点調査と、1〜3地点の比較を最優先する。
- データ状態と根拠情報を正しく区別できない場合、見た目の完成を優先して推測値を表示しない。
- 将来機能のための空画面、認証、永続データベースおよび定期更新基盤を作らない。
- 外部サービスや既存の公式配信を活用し、ハッカソン内で全国GIS基盤を一から構築しない。
- 必須機能が完成する前に、拡張機能の実装へ進まない。
- 外部APIのレート制限は必要だが、主要導線が動作した後の終盤タスクとし、初期開発を妨げない。
